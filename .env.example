# Put your Google AI Studio key here: https://aistudio.google.com/apikey
# This file is gitignored. Never commit it and never deploy it to GitHub Pages —
# anything served from a public site can be read by anyone who visits it.
#
# NOTE: this file is only read when the page is served over http.
# Run:  node serve.js    then open http://localhost:3000
# Opening index.html straight off disk (file://) cannot read it — use Settings instead.

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
