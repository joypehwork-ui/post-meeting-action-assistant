# Your OpenCode API key. Get one from the workshop facilitator.
# This file is the committed template; the real key goes in .env, which is gitignored.
#
# The key is read by serve.js and never sent to the browser.
# Run:  node serve.js    then open http://localhost:3000

OPENCODE_API_KEY=
LLM_MODEL=deepseek-v4.1-flash
